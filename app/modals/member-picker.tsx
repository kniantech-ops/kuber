import { AppScreen } from '@/components/ui/AppScreen';
import { AppText } from '@/components/ui/AppText';

export default function MemberPickerModal() {
  return (
    <AppScreen>
      <AppText>Member picker modal scaffolded.</AppText>
    </AppScreen>
  );
}
