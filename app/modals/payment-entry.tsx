import { AppScreen } from '@/components/ui/AppScreen';
import { AppText } from '@/components/ui/AppText';

export default function PaymentEntryModal() {
  return (
    <AppScreen>
      <AppText>Payment entry modal scaffolded.</AppText>
    </AppScreen>
  );
}
