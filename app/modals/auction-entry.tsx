import { AppScreen } from '@/components/ui/AppScreen';
import { AppText } from '@/components/ui/AppText';

export default function AuctionEntryModal() {
  return (
    <AppScreen>
      <AppText>Auction entry modal scaffolded.</AppText>
    </AppScreen>
  );
}
